services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - REDIS_HOST=redis
      - DATABASE_URL=
      - JWT_SECRET=
      - EMAIL_USER=
      - EMAIL_PASS=
      - PORT=3000
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6380:6379"
